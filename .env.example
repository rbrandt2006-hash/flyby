# Flyby AI — frontend configuration (copy to .env)

# Backend base URL. Leave blank for local development — the Vite dev server
# proxies backend paths to http://localhost:8659.
# Set this only when the frontend and backend are on different hosts.
VITE_API_URL=""
